/*

class Oddball extends Normal {

  Oddball(){
    super(speed, y, spd, ang, r, g, b);
  }

  void move() {
    x_pos = x_pos + speed + (double)Math.cos(radians((float)angle));
    y_pos = y_pos + speed + (double)Math.sin(radians((float)angle));
  }

  void show() {
    ;
  }
}

*/
